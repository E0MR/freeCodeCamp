1.What are pseudo-classes?

    They are selectors used to add movement effects to an element during interactions.

    They are CSS rules that create additional content within an element when needed.

    They are properties that adjust an element's size or layout on the page.

    They are keywords added to a selector that style an element based on its state.
    Correct!


2.Which pseudo-class applies when a pointing device is being positioned over an element?

    :active

    :hover
    Correct!

    :checked

    :focus


3.Which pseudo-element allows you to style the first letter of a paragraph?

    :first-letter

    :first-child

    ::first-letter
    Correct!

    :last-of-type


4.Which pseudo-class changes the style of an element while it is being clicked?

    :focus

    :active
    Correct!

    :checked

    :hover


5.Which pseudo-class is used to style an element when it is ready to receive user input, such as a text field being clicked or tabbed into?

    :focus
    Correct!

    ::focus

    :active

    :visited


6.Which of the following CSS rules correctly adds the text Note: in front of each paragraph element with a class of note?

    p.note::before {
        content: "Note:";
    }
    Correct!

    p::before {
        content: "Note:";
    }

    p.note::after {
        content: "Note:";
    }

    .note::before {
        content: "Note:";
    }


7.Which pseudo-class applies to an input field when it is selected or toggled on?

    :required

    :optional

    :disabled

    :checked
    Correct!


8.Which of the following is the correct syntax to style the last child of a list?

    .li:last-child() {
        color: blue;
    }

    #li:last-child {
        color: blue;
    }

    li:last-child {
        color: blue;
    }
    Correct!

    li:nth-child(last) {
        color: blue;
    }


9.Which pseudo-class targets input fields that are not required to fill out?

    ::optional

    :required

    :enabled

    :optional
    Correct!


10.What does the :disabled pseudo-class do?

    It styles checked inputs.

    It styles elements being hovered over.

    It styles elements that are not available for user interaction.
    Correct!

    It selects elements that do not match a given selector.


11.Which pseudo-class applies when a form input meets its validation criteria?

    :disabled

    :required

    :checked

    :valid
    Correct!


12.Which one of these is not a location pseudo-class?

    :visited

    :any-link

    :link

    :current
    Correct!


13.Which of the following selects the third list item?

    li:nth-child(three) {
        color: red;
    }

    li:child(3) {
        color: red;
    }

    li:last-child(3) {
        color: red;
    }

    li:nth-child(3) {
        color: red;
    }
    Correct!


14.Which elements will have a color of blue with the following CSS?
    p:is(.blue, .highlight) {
        color: blue;
    }

    <p>Paragraph 1</p>
    <span class="highlight">Paragraph 2</span>

    <p class="blue">Paragraph 1</p>
    <p class="highlight">Paragraph 2</p>
    Correct!

    <div class="blue">Paragraph 1</div>
    <div class="highlight">Paragraph 2</div>

    <p class="class">Paragraph 1</p>
    <p class="highlight">Paragraph 2</p>


15.What does the :not() pseudo-class do?

    It adds styles to all elements.

    It selects elements that do not match a given selector.
    Correct!

    It selects elements that match a given selector.

    It selects all child elements of a parent.


16.What does the following CSS rule do?
    p:first-of-type {
        font-style: italic;
    }

    It selects all p elements in the document.

    It selects the first p element in the document.

    It selects the first p element within a parent container.
    Correct!

    It selects the first child of every p element.


17.What does the :last-of-type pseudo class do?

    It selects the last child element of a specific type within its parent.
    Correct!

    It selects the middle child element of a specific type within its parent.

    It selects the first child element of a specific type within its parent.

    It selects every child element of a specific type within its parent.


18.Which pseudo-class is used to select the second item in a list?

    :is()

    :nth-child(2)
    Correct!

    :first-child

    :required


19.Which one of these is a functional pseudo-class?

    :first-child

    :match()

    :is()
    Correct!

    :checked


20.Which one of these is not a functional pseudo-class?

    :has()

    :not()

    :contains()
    Correct!

    :where()

